<h1>Most Active page</h1>

<%= link_to 'Dashboard(Home)', admin_path, class: "btn btn-info" %>

<table border=0 cellspaceing="5" cellpadding="5">
	<tr>
		<td>User Name</td>
		
		<td>Time Spent</td>
	</tr>
	<% @userlogs.each do |userlog| %>
		<tr>
			<td><%= userlog.page.title %></td>
			
			<td><%= userlog.total  %>	
			
		</tr>
	<% end %>
</table>

 <br><br>
<%= will_paginate @userlogs %>