

<%= form_for resource, :as => resource_name, :url => invitation_path(resource_name), :html => {:method => :post,class:"form-signin form-invitation"} do |f| %>
<h3><%= t "devise.invitations.new.header" %><%= %></h3>

  <%= devise_error_messages! %>

<% resource.class.invite_key_fields.each do |field| -%>
  
  <%= f.text_field field, class: "form-control", :autofocus => true , :placeholder=>"Email"%></p>
  
<% end -%>

 <%= f.submit t("devise.invitations.new.submit_button") , class: "btn btn-lg btn-login btn-block" %> <%= link_to "Cancel", admin_path , class: "btn btn-lg btn-login btn-block" %>
<% end %>

